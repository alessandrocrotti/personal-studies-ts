# For details on buf.yaml configuration, visit https://buf.build/docs/configuration/v2/buf-yaml
version: v2
modules:
  - path: ./src/networking/api-paradigms/grpc/protos # Path to your proto files
lint:
  use:
    - STANDARD
breaking:
  use:
    - FILE
